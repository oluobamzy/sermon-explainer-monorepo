// ============================================================
// SERMON EXPLAINER - Frontend Configuration
// ============================================================
// This is the ONLY file you should need to edit when your Render
// backend becomes available. Everything else in this project reads
// from this one setting - so there's exactly one place to update.
//
// Right now this points at "localhost" (your own computer) so you
// can test the frontend against your backend while it's still running
// locally, before Render is set up.
//
// Once your backend is deployed to Render, it will give you a URL
// that looks like: https://your-service-name.onrender.com
// Replace the value below with that exact URL (no trailing slash).
// ============================================================

const API_BASE_URL = "http://localhost:8000";
